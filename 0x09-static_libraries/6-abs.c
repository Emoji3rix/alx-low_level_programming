#include "main.h"

/**
 * _abs - computes the absolute function of an integer
 * @n: the integer to be computed
 * Return: the absolute values of an integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
